import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#267ddb", // Sky Blue
    },
    secondary: {
      main: "#ffffff", // White
    },
  },
});

export default theme;
